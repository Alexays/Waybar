sudo: false

services:
    - docker

env:
    - distro: debian

before_install:
    - docker pull alexays/waybar:${distro}

script:
    - docker run alexays/waybar:${distro} /bin/sh -c "cd /root && TRAVIS=true meson builddir && ninja -C builddir test"